#give your cluster a name.
cluster.name: my-cluster
#give your nodes a name (change node number from node to node).
node.name: "es-node-1"
#define node 1 as master-eligible:
node.master: true
#define nodes 2 and 3 as data nodes: node.data: true enter the private IP and port of your node:
network.host: 0.0.0.0
http.port: 9200
#detail the private IPs of your nodes: discovery.zen.ping.unicast.hosts: ["157.230.1.8", "157.230.6.129"] discovery.zen.minimum_master_nodes: 1